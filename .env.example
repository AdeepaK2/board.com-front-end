# WebSocket server URL for production

# WebSocket server URL for production. When using a TLS-terminating proxy (nginx, DO App Platform)
# point to the /ws path on your app domain so the proxy can forward upgrades to the backend.
# Example (recommended):
VITE_WEBSOCKET_URL=wss://your-backend-url.ondigitalocean.app/ws

# For local development, you can leave this empty or use:
# VITE_WEBSOCKET_URL=ws://localhost:8080

# For local development, you can leave this empty or use:
# VITE_WEBSOCKET_URL=ws://localhost:8080